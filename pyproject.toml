[project]
name = "ytm"
version = "0.1.0"
description = "CLI for browsing your YouTube Music library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rich>=15.0.0",
    "ytmusicapi>=1.12.3",
]
